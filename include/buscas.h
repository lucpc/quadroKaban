#ifndef BUSCAS_H
#define BUSCAS_H
#include <vector>

using namespace std;

template <typename T>
int buscaSequencial(vector<T>& v, T& x);

template <typename T>
void buscaBinaria(vector<T>& v, T& x);



#endif // BUSCAS_H
